import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  reactStrictMode: true,
  // Demo lưu dữ liệu ở localStorage nên không cần server-side gì thêm
};

export default nextConfig;
